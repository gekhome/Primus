@using System.Collections
@using Primus.Models
@using Primus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .DataValueField("EIDIKOTITA_ID")
    .DataTextField("EIDIKOTITA_TEXT")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("Eidikotites_Read", "School");
        }).ServerFiltering(true);
    })
)
@Html.ValidationMessageFor(m => m)