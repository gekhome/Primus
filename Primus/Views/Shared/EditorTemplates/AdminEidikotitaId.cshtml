@using System.Collections
@using Primus.Models
@using Primus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(false)
    .DataValueField("EIDIKOTITA_ID")
    .DataTextField("EIDIKOTITA_TEXT")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("EidikotitesRead", "Admin").Data("filterEidikotites");
        }).ServerFiltering(true);
    })
    .CascadeFrom("ΣΧΟΛΗ")
)
@Html.ValidationMessageFor(m => m)